using System;
using System.Collections.Generic;
using System.Text;

namespace FootballManager.Domain.SeedWork
{
    /// <summary>
    /// This class indicates an aggregate root in DDD sense, so a base object of our domain.
    /// This is an empty interface used only to mark which items might be used as roots
    /// </summary>
    interface IAggregateRoot
    {
    }
}
